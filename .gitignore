__pycache__/
*.pyc
.venv/
tools/m1-*.txt
node_modules/

# The Norwegian development record. Kept locally (decision #13 -- not rewritten),
# excluded from the public repo: it names the account, the author, and home
# paths on every decision line. See CP-PUB / FASIT-cppub.md.
TODO.md
tests/gold/FASIT-*.md
# Snapshots of exactly those two, taken by tools/backup_untracked.py. They
# hold the same real paths, so they are excluded for the same reason -- and
# a backup that leaked what the original is kept out for would be worse than
# no backup at all.
.todo-backups/
